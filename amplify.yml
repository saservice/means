version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - echo 'Build step is not required for this project'
  artifacts:
    baseDirectory: .  # プロジェクトのルートディレクトリを指定
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

